{
  "name": "roadmap-airtable",
  "version": "1.0.1",
  "private": true,
  "description": "Leaflet map + Airtable backend (categories + subcategories + navigation to Google Maps)",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
